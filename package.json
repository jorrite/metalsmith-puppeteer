{
  "name": "metalsmith-puppeteer",
  "description": "A metalsmith plugin to to use puppeteer.",
  "repository": "git://github.com/jorrite/metalsmith-puppeteer.git",
  "version": "0.1.6",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "test": "npm run build && mocha test/index.js --timeout 15000",
    "build": "babel src --out-dir dist",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "assert-dir-equal": "^1.1.0",
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-async-to-generator": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "handlebars": "^4.0.10",
    "metalsmith": "^2.3.0",
    "metalsmith-layouts": "^1.8.1",
    "metalsmith-markdown": "^0.2.1",
    "mocha": "^4.1.0"
  },
  "dependencies": {
    "async": "^2.6.0",
    "babel-polyfill": "^6.26.0",
    "debug": "^3.2.0",
    "http": "0.0.0",
    "multimatch": "^2.1.0",
    "puppeteer": "^0.13.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
